# Restricted Boltzmann Machine

Although simple, this model generates very neat images

Training took for only 10 epochs on a CPU, with a learning rate of 1e-2.


Check out ./output/real for real images

And ./output/fake for images generated by the RBM.
